if uiRefresh
  {  uiRefresh=false;
     if isFocused && uiPopupKeyboard 
      with (ui_keyboard)
         { //if other.uiTabOrder>0 {uiEnterStr='Next';}
           uiKeyboardShow=true; 
         }  
  }

